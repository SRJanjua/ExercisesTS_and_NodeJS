   //10.
  //Adding Comments
 // 1. first program
//following lines taken from Q7.

  var operand1:number = 5;
  var operand2:number = 3;
  console.log("Re-Using Addition Program in Q7; Result is : " + (operand1 + operand2) + ", along with comments"); 
 
 /*in the statement above, inside the log function, I had  used parseInt function (in js) to
   make the two operands act like integers and add together, which later concatenate with the
   string "Addition "*/
